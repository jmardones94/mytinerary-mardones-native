const itinerariesReducer = (
  state = { itineraries: [], comments: [], activities: [] },
  action
) => {
  switch (action.type) {
    default:
      return state
  }
}

export default itinerariesReducer
